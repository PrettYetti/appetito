<div class="col-sm-offset-4 col-sm-4">
<h1>New User</h1>
<%= form_for(@user) do |form| %>
	<%= render form %>
	<div class="actions">
	    <%= form.submit %>
	  </div>
<% end %>

<%= link_to 'Back', users_path %>
