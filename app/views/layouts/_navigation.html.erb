<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "appetito", root_path, class: "navbar-brand" %>
    </div><!-- /.navbar-header -->
    <!-- Collect the nav links, forms, and other content for toggling -->
   
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
        <li class="dropdown" id="link_to_home">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              </span><%= current_user.name %>
              <span class="caret"></span>
              <%= image_tag current_user.avatar.url(:icon), class: "avatar avatar-nav" %>
              <span class="badge notification-badge"><%= current_user.notifications.where(notified: false).count %>
            </a>
            <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Homepage", user_path(current_user) %></li>
            <li><%= link_to "Logout", logout_users_path %></li>
          </ul>
          </li>
        </ul>
        <% else %>
      </ul>
      <%= render 'events/login' %>
      <% end %>
  </div><!-- /.container-fluid -->
</nav>

